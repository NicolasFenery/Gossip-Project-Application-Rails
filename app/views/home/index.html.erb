<div class="jumbotron">
  <h1 class="display-4">The Gossip Project</h1>
  <p class="lead">Bienvenue, check les secrets les plus tendance de ta région.</p>
</div>

<% @gossips.each do |gossip| %>

<div class="container">
	<div class="row my-3">
		<div class="col-sm">
			<div class="card border border-primary" style="width: 18rem;">
			  <div class="card-body">
			    <h5 class="card-title"><%= gossip.title %></h5>
			    <h6 class="card-subtitle mb-2 text-muted"><%= gossip.user.first_name %></h6>
			    <p class="card-text"><%= link_to "Clique Ici pour voir le Secret", gossip_path(gossip.id) %></p>
			  </div>
			</div>
		</div>
	</div>
</div>

<% end %>